<p style="color: green"><%= notice %></p>

<%= render @donation %>

<div>
  <%= link_to "Edit", edit_donation_path(@donation), class:"btn btn-dark" %> 
  
  <%= link_to "Delete", @donation, method: :delete, class:"btn btn-danger" %>
<br>
  <%= link_to "Back to donations", donations_path %>
 
</div>
